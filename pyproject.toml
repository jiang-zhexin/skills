[project]
name = "skills"
version = "1.0.0"
description = "jiang-zhexin's skills"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "seedir>=0.5.1",
]

[dependency-groups]
dev = [
    "ruff>=0.15.14",
    "ty>=0.0.39",
]
